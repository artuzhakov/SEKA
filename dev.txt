🏗️ СХЕМА РАЗРАБОТКИ НА ЛОКАЛКЕ
📋 ОБЩАЯ АРХИТЕКТУРА ПРОЦЕССА:
text
GitHub (origin) ←→ Твой локальный компьютер (main) ←→ Мои PR (feature branches)
🔄 ТВОЙ РАБОЧИЙ ПРОЦЕСС:
1. Ежедневное начало работы:
bash
# Переключиться на main ветку
git checkout main

# Скачать последние изменения с GitHub
git pull origin main

# Установить обновленные зависимости (если нужно)
composer install
npm install

# Запустить миграции (если были изменения БД)
php artisan migrate

# Запустить окружение
php artisan serve
npm run dev
2. Когда я создаю PR:
bash
# Получить информацию о новых PR
git fetch origin

# Посмотреть список PR веток
git branch -r | grep pull

# Переключиться на ветку PR для тестирования
git checkout -b test-pr origin/feature/game-logic

# Протестировать изменения
php artisan test
npm run dev
# Проверить функционал в браузере
3. После одобрения PR в GitHub:
bash
# Вернуться на main ветку
git checkout main

# Получить обновленный main с изменениями из PR
git pull origin main

# Удалить локальную тестовую ветку
git branch -d test-pr
🎯 КОНКРЕТНЫЕ ДЕЙСТВИЯ ДЛЯ ТЕСТИРОВАНИЯ:
Для бэкенд изменений:
bash
# Запустить тесты
php artisan test

# Проверить миграции
php artisan migrate:status

# Проверить роуты
php artisan route:list

# Запустить сервер
php artisan serve
Для фронтенд изменений:
bash
# Собрать ассеты
npm run build

# Или запустить dev сервер
npm run dev

# Проверить линтинг
npm run lint
🔧 НАСТРОЙКА ЛОКАЛЬНОГО ОКРУЖЕНИЯ:
1. Файл .env:
env
APP_ENV=local
APP_DEBUG=true

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=seka
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=pusher
PUSHER_APP_ID=your_app_id
PUSHER_APP_KEY=your_app_key
PUSHER_APP_SECRET=your_app_secret
2. Команды для первого запуска:
bash
# Установка зависимостей
composer install
npm install

# Генерация ключа
php artisan key:generate

# Миграции и сиды
php artisan migrate --seed

# Сборка фронтенда
npm run build
🚨 ЧЕК-ЛИСТ ПЕРЕД МЕРЖЕМ PR:
Тесты проходят: php artisan test

Фронтенд собирается: npm run build

Миграции работают: php artisan migrate

Функционал проверен в браузере

Нет конфликтов с main веткой

📞 КОММУНИКАЦИЯ:
Если发现问题:
Оставить комментарий в PR на GitHub

Указать конкретные строки с проблемами

Опиши шаги для воспроизведения бага

Если все ок:
Approve PR в интерфейсе GitHub

Нажать "Merge pull request"

Удалить ветку после мержа

🎪 ЧТО Я БУДУ ДЕЛАТЬ В КАЖДОМ PR:
✅ Детальное описание изменений

✅ Инструкция по тестированию

✅ Список затронутых файлов

✅ Тесты для новой функциональности

✅ Скриншоты если есть UI изменения